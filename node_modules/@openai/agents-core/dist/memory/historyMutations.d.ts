import type { AgentInputItem } from '../types';
import type { SessionHistoryMutation } from './session';
/**
 * Applies persisted-history mutations and returns a new canonical item list.
 */
export declare function applySessionHistoryMutations(items: AgentInputItem[], mutations: SessionHistoryMutation[]): AgentInputItem[];
