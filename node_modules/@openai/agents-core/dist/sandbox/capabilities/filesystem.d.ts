import { type Tool } from '../../tool';
import { Capability, type ConfigureCapabilityTools } from './base';
export type FilesystemArgs = {
    configureTools?: ConfigureCapabilityTools;
};
declare class FilesystemCapability extends Capability {
    readonly type = "filesystem";
    private readonly configureTools?;
    constructor(args?: FilesystemArgs);
    tools(): Tool<any>[];
}
export type Filesystem = FilesystemCapability;
export declare function filesystem(args?: FilesystemArgs): Filesystem;
export {};
