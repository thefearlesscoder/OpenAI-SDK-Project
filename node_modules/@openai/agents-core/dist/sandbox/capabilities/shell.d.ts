import { type Tool } from '../../tool';
import { Capability, type ConfigureCapabilityTools } from './base';
export type ShellArgs = {
    configureTools?: ConfigureCapabilityTools;
};
declare class ShellCapability extends Capability {
    readonly type = "shell";
    private readonly configureTools?;
    constructor(args?: ShellArgs);
    tools(): Tool<any>[];
    instructions(): string;
}
export type Shell = ShellCapability;
export declare function shell(args?: ShellArgs): Shell;
export {};
