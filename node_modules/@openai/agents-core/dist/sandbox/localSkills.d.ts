import { type LocalDirLazySkillSource } from './capabilities/skills';
export type LocalDirLazySkillSourceOptions = {
    /**
     * Local directory containing one child directory per skill.
     */
    src: string;
    /**
     * Base directory used to resolve relative src paths when discovering SKILL.md metadata.
     * Defaults to the current working directory.
     */
    baseDir?: string;
};
export declare function localDirLazySkillSource(srcOrOptions: string | LocalDirLazySkillSourceOptions): LocalDirLazySkillSource;
