import { Manifest } from '../manifest';
import type { SandboxSessionLike, SandboxSessionState } from '../session';
export declare function applyManifestToProvidedSession(session: SandboxSessionLike<SandboxSessionState>, manifest: Manifest, runAs?: string): Promise<void>;
