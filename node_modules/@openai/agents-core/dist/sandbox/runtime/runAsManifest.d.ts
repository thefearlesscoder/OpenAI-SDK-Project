import { Manifest } from '../manifest';
import type { SandboxUser } from '../users';
export declare function sandboxRunAsName(runAs: string | SandboxUser | undefined): string | undefined;
export declare function manifestWithRunAsUser(manifest: Manifest, runAs: string | SandboxUser | undefined): Manifest;
