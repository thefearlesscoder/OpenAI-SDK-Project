import { arraysEqual } from "./compare.mjs";
export const DEFAULT_REMOTE_MOUNT_COMMAND_ALLOWLIST = [
    'ls',
    'find',
    'stat',
    'cat',
    'less',
    'head',
    'tail',
    'du',
    'grep',
    'rg',
    'wc',
    'sort',
    'cut',
    'cp',
    'tee',
    'echo',
    'mkdir',
    'rm',
];
export function isDefaultRemoteMountCommandAllowlist(allowlist) {
    return arraysEqual(allowlist, DEFAULT_REMOTE_MOUNT_COMMAND_ALLOWLIST);
}
export function hasCustomRemoteMountCommandAllowlist(allowlist) {
    return (allowlist.length > 0 && !isDefaultRemoteMountCommandAllowlist(allowlist));
}
//# sourceMappingURL=remoteMountCommandAllowlist.mjs.map