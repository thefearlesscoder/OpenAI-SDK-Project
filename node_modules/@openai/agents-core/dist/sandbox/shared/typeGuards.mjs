export function isRecord(value) {
    return typeof value === 'object' && value !== null && !Array.isArray(value);
}
export function isStringRecord(value) {
    return (isRecord(value) &&
        Object.values(value).every((entry) => typeof entry === 'string'));
}
export function readOptionalString(value, key) {
    const result = value[key];
    return typeof result === 'string' ? result : undefined;
}
export function readString(value, key, fallback = '') {
    const result = value[key];
    return result === undefined || result === null ? fallback : String(result);
}
export function readOptionalNumberArray(value) {
    return Array.isArray(value)
        ? value.filter((entry) => typeof entry === 'number')
        : undefined;
}
export function readStringArray(value) {
    return Array.isArray(value)
        ? value.filter((entry) => typeof entry === 'string')
        : [];
}
//# sourceMappingURL=typeGuards.mjs.map