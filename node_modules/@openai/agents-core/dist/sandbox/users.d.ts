export type SandboxUser = {
    name: string;
};
export type SandboxUserInit = string | SandboxUser;
export type SandboxGroup = {
    name: string;
    users?: SandboxUser[];
};
export type SandboxGroupInit = {
    name: string;
    users?: SandboxUserInit[];
};
export type SandboxEntryGroup = SandboxUser | SandboxGroup;
export declare function normalizeUser(user: SandboxUserInit): SandboxUser;
export declare function normalizeGroup(group: SandboxGroupInit): SandboxGroup;
export declare function normalizeEntryGroup(group: SandboxEntryGroup): SandboxEntryGroup;
