import { Model } from '@openai/agents-core';
import type { ModelRetryAdvice, ModelRetryAdviceRequest, ModelRequest, ModelResponse, ResponseStreamEvent } from '@openai/agents-core';
import OpenAI from 'openai';
export declare const FAKE_ID = "FAKE_ID";
/**
 * A model that uses (or is compatible with) OpenAI's Chat Completions API.
 */
export type OpenAIChatCompletionsModelOptions = {
    /**
     * When true, reject Responses-only features that Chat Completions cannot
     * honor. Defaults to false, which preserves the previous ignore-and-warn
     * behavior.
     */
    strictFeatureValidation?: boolean;
};
export declare class OpenAIChatCompletionsModel implements Model {
    #private;
    constructor(client: OpenAI, model: string, options?: OpenAIChatCompletionsModelOptions);
    getRetryAdvice(args: ModelRetryAdviceRequest): ModelRetryAdvice | undefined;
    getResponse(request: ModelRequest): Promise<ModelResponse>;
    getStreamedResponse(request: ModelRequest): AsyncIterable<ResponseStreamEvent>;
}
